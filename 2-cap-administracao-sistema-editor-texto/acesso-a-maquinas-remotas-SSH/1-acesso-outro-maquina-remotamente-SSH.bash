# O SSH (Secure Shell ou shell seguro) é a evolução do telnet, permitindo toda
# a praticidade de um acesso remoto com uma diferença: um túnel criptografado,
# garantindo a privacidade e evitando ataques.
# substituindo o telnet

# OpenSSH, configurador de um serviço SSH.

# SSH é o cliente para acesso a terminais remotos.
#   @user ( nome do usuario ) 
#   o nome o ip do host
#   porta padrão 22, se não tiver na porta padrão usar o param -p 


ssh @user 000.000.0.000